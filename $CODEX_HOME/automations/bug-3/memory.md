2026-03-24 04:10 CST run: processed BUG-18051. Fixed `src/main.rs` auto storage selection to fail fast when no persistent backend is configured, kept explicit `CREDBRIDGE_STORAGE_BACKEND=memory` for dev/test, added 4 regression tests, ran `cargo test auto_backend --bin vault-service`, `cargo test --bin vault-service`, and `cargo test --test credentials_api_tests` successfully, updated `zentao/18051.md` and `zentao/my-bug-todo.md` to `[3]`, and committed on `branch_dev` as `5ad9edd fix: fail fast on non-persistent auto storage`.
