# CredBridge US Production Configuration
# 从 Vault 读取的机密用 ${VAR_NAME} 占位符表示

server:
  host: "0.0.0.0"
  port: 8080

# 数据库配置 - US Region
database:
  url: "${DATABASE_URL}"
  pool_size: 50
  timeout_seconds: 30

# Redis 缓存
redis:
  url: "${REDIS_URL}"
  pool_size: 50

# Vault 配置
vault:
  addr: "https://vault-us.example.com"
  token: "${VAULT_TOKEN}"
  kv_path: "secret/prod"

# 日志配置
log:
  level: "warn"
  format: "json"

# 安全配置
security:
  jwt_secret: "${JWT_SECRET}"
  token_expiry: 7200
  encryption_key: "${ENCRYPTION_KEY}"

# CORS 配置
cors:
  allowed_origins:
    - "https://credbridge-us.example.com"
    - "https://admin.credbridge-us.example.com"

# API 限流
rate_limit:
  requests_per_minute: 10000
  burst: 100

# 监控配置
metrics:
  enabled: true
  port: 9090
  path: "/metrics"
