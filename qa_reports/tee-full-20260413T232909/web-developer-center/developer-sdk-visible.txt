开发者中心 / SDK 示例 可见关键文本摘录

- npm install @toani/vault-sdk
- 包名已从 @credbridge/sdk 迁移到 @toani/vault-sdk
- 基础使用示例
- const sdk = new ToaniVaultSDK({
- baseUrl: 'http://localhost:8082',
- token: 'v4.local.your-token-here',
- Token 引导
- 直接使用 sandbox token 调用保留接口
- const response = await fetch('http://localhost:8082/api/v1/sandbox/stats', {
- Authorization: 'Bearer <SANDBOX_TOKEN>'
- 沙箱浏览器自动化
- credential_id: 'your-credential-id'
- await sdk.sandbox.navigate(session.id, 'https://example.com/login');
- await sdk.sandbox.fill(session.id, '#username', { $credential: 'username' });
- await sdk.sandbox.takeScreenshot(session.id, {
- await sdk.sandbox.exportData(session.id, {

来源:
- live page snapshot: `sdk-tab-snapshot.yml`
- screenshot: `developer-sdk-tab.png`
